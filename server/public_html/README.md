# public_html custom templates — LiquidWeb Server (Pull Required)

This directory is a placeholder. Files to pull from LiquidWeb:

- `ms-home-tpl.php`
- `mockup-home.html`
- Any `ms-*.php` in `/home/mindstream/public_html/`

**Server:** adroisites.com
**Action:** Store real `liquidweb-whm-token` in GCP Secret Manager, then re-run pull.
