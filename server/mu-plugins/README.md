# mu-plugins — LiquidWeb Server (Pull Required)

This directory is a placeholder. Contents must be pulled from LiquidWeb.

**Source path:** `/home/mindstream/public_html/wp-content/mu-plugins/`
**Server:** adroisites.com
**Action:** Store real `liquidweb-whm-token` in GCP Secret Manager, then re-run pull.
